# Playback latency verification

Issue #18 remains open until browser retest confirms whether build `20260905-4` removes the initial `BUFFERING → UNSTARTED` reset and materially reduces click-to-PLAYING time. The dedicated `playback-diagnostics.html` page is the verification source.