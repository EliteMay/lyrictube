diff --git a/作業報告書.md b/作業報告書.md
--- a/作業報告書.md
+++ b/作業報告書.md
@@ -870,2 +870,10 @@
 ### 変更
-
+- 公開GitHub上のREADME・作業報告・ログイン画面から個人名、具体的なユーザー名、パスワード記載を削除。
+- `data/site-config.json` から旧簡易アクセス用パスワード項目を削除。
+- ログイン画面でアカウント名の既定値を使わず、アカウント名とパスワードの両方を必須化。
+- 旧オーナー用フォールバックファイル名を `data/library-owner.json` に一般化。
+- Supabase上のアカウント・曲データ・プレイリストは変更しない。
+
+### 注意
+- 公開リポジトリの過去コミット履歴に以前の記載が残る可能性があるため、現在の認証情報はGitHubへ再掲載しない。
+
@@ -898,11 +906,9 @@
 ### 確認
 - `tags.js` は反映前に `node --check` で構文確認。
 - 既存のGitHub Actions検査ワークフローは権限制約により変更していない。
-- 公開GitHub上のREADME・作業報告・ログイン画面から個人名、具体的なユーザー名、パスワード記載を削除。
-- `data/site-config.json` から旧簡易アクセス用パスワード項目を削除。
-- ログイン画面でアカウント名の既定値を使わず、アカウント名とパスワードの両方を必須化。
-- 旧オーナー用フォールバックファイル名を `data/library-owner.json` に一般化。
-- Supabase上のアカウント・曲データ・プレイリストは変更しない。
-
-### 注意
-- 公開リポジトリの過去コミット履歴に以前の記載が残る可能性があるため、現在の認証情報はGitHubへ再掲載しない。
+- `index.html` が `tags.css?v=35` と `tags.js?v=35` を読み込むことを確認。
+- GitHub Pages v35 build / deployment成功。
+
+### 未確認
+- 実機スマホでのタグ管理画面の最終タップ操作。
+- 実ブラウザ上で108曲すべてを使った長時間操作時のスクロール感。
