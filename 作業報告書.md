## 2026-09-03 Account registration scroll fix

- User report: 新規アカウント作成画面で下まで移動できない。
- Cause: Registration Form追加後もAccess Cardに低Viewport時の縦Scroll contractがなかった。
- Fix: `auth-ui.css` の `.access-card` にdynamic viewport max-height + overflow-yを追加。
- Build: `20260903-1`
- Storage / Schema / Supabase: 変更なし。
- Visual verification: 実ブラウザ未確認。CI / Pages後にユーザー環境で確認予定。

## 2026-09-01 サイト内アカウント作成

- 学校PC等の別端末でゲストログインすると曲追加ができないという利用状況を起点に、サイト内からクラウドアカウントを作成できる導線を追加。
- ログイン画面から登録フォームへ切り替え、作成キーを知っている利用者だけ新規登録可能にした。
- 作成キーはFrontendへHardcodeせず、Supabase Edge Function側でDigest照合。誤入力にはRate Limitを追加。
- DB側はAccount作成と初期State作成を同一Transactionにし、途中失敗で片方だけ残らないようにした。
- 登録成功後はSessionを発行して自動ログインする。
- Build `20260901-1`。Data Schema 4は変更なし。
- Static / CI検証を行う。実ブラウザでの登録画面Visualと実際の学校PC環境は未確認。

## web-project-guide 定期監査導入（2026-08-31）

### 目的

`EliteMay/web-project-guide` の更新を見逃さず、LyricTubeを定期的に現行Guideへ照らして確認できるようにする。

### 変更した内容

- `project-guide.json` を追加し、確認済みGuide Version / Commit / Project Profileを記録。
- `tools/check_web_project_guide.py` を追加し、Guideの最新 `guide-version.json` とmain Commitを取得してbaselineと比較。
- `.github/workflows/guide-audit.yml` を追加。
  - 毎週月曜 00:23 UTC（日本時間09:23頃）に実行。
  - `workflow_dispatch` で手動実行可能。
  - 定期実行時は既存 `Validate LyricTube` も再利用して実行。
  - 未確認のGuide変更があればGitHub Issueを1件作成または更新。
  - baseline一致後は監査Issueを自動Close。
- `.github/workflows/validate-js.yml` に `workflow_call` を追加し、通常Validationを重複定義せず再利用可能にした。
- `PROJECT_LEARNINGS.md` を追加し、Player Controller / Version hardcode / Sidebar Layout等の再発防止知識を記録。
- `docs/GUIDE_AUDIT.md` を追加し、定期監査の方針と運用を記録。
- READMEを現行構成に合わせて更新。

### 安全方針

- 定期監査はGuideの変更検出とLyricTubeのValidationを行うが、アプリコード・UI・保存形式を自動変更しない。
- Guide差分を確認して必要な変更だけ別作業として反映する。
- Guide更新による大規模変更を無条件に自動適用しない。

### 保存・互換性への影響

- `lyrictube.library.v3` 変更なし。
- Data Schema 4変更なし。
- localStorage / IndexedDB Key変更なし。
- ユーザー向けVersion / Build変更なし。

### 確認予定

- Pull Request上で `Validate LyricTube` と `Web Project Guide Audit` の初回実行を確認。
- Merge後の最終main CommitでもCI結果を確認。
- GitHub Pagesの公開内容自体にはRuntime変更がないため、表示仕様への影響なし。

## v0.13.2 Sidebar安定化 / build 20260830-8（2026-08-30）

- `web-project-guide` の既存サイト修正ルート、F-006 / F-012、UI / UX / Quality Checklistを基準に修正。
- Sidebarをブランド・中央Scroll・下部操作の3領域へ分離。
- 低い画面やモバイルで `設定 / ? / 書き出し / 読み込み` が画面外へ押し出される問題を修正。
- タグ / プレイリスト / 曲一覧のNested Scrollを減らし、中央の単一Scroll領域へ集約。
- Sidebar見出しの視認性を改善。
- `tests/sidebar-layout.test.js` を追加。
- 保存形式 / Storage Key / Schema変更なし。
- Static Validation対象。実ブラウザの低い縦解像度・125〜150% Zoomは未確認。

## v0.13.2 Player Controller仕上げ（2026-08-30）

- 下部シークバーに残っていたYouTube直接シークを共通Player Controller経路へ変更。
- Local Media側の重複シークListenerを削除。
- Player Controller移行後に不要になった旧 `restartLocal` shimを削除。
- 再発防止Guardを追加。
- 保存形式 `lyrictube.library.v3` / Schema 4は変更なし。

## v0.13.1 Player Controller移行（2026-08-30）

- `core/player-controller.js` を追加。
- YouTube / Local Mediaを同じ再生契約へ統合。
- Local Mediaが `currentPlayerTime / duration / state / toggle / seek / restart / playback rules / bottom player` を後から上書きする構造を撤去。
- Local Media側に残す互換Patchは曲追加・バージョン編集Dialogの2か所だけに縮小。
- 開始/終了、スキップ、同期エディタ、キーボード操作を共通Player経路へ統一。
- `player-controller.test.js` と回帰Guardを追加。
- 保存形式 `lyrictube.library.v3` / Schema 4は変更なし。

## v0.13.0 段階リファクタ Phase 1（2026-08-30）

- `core/app-utils.js` を追加し、LRC・時刻・文字列・同期保持のPure utilityを `app.js` から分離。
- `core/runtime-hooks.js` を追加。
- `tags.js` に残っていた `viewSongs / renderBrowse / renderAll / renderMainPage` の関数上書きを廃止。
- タグ絞り込みはFilter、描画拡張はRender Hook、タグ画面はhandled Hookへ移行。
- Utility / HookのNodeテストを追加。
- 保存形式 `lyrictube.library.v3` / Schema 4は変更なし。

## v0.12.0 安定性修正（2026-08-30）

### 修正した内容

- Local Media / Tagsの30秒Polling初期化を廃止し、`lyrictube:app-ready`イベントへ変更。
- MP3 / MP4の同期歌詞追従、歌詞クリックシーク、Space / ← / →操作を共通Player経路へ修正。
- Local Mediaへ小さな再生API（play / pause / seek / time / duration / state）を追加。
- `site-shell.js`に残っていた`GH v35`再表示コードを削除。
- JSON書き出し名の旧`v14`表記を撤去。
- `guest.css`の二重読込処理を削除。
- 壊れたlocalStorage JSONを復旧キーへ退避してから空ライブラリへ移るよう変更。
- Schema検査をID重複、Playlist参照、時間範囲、Skip範囲、Tag参照まで拡張。
- PC / MobileのSidebar下部4操作を4列固定。
- 現行仕様に合わせてHelpを更新。
- 回帰テストを追加。

### 完了条件の確認

- 既存のYouTube / Local Media / 歌詞 / Quick Sync / Cloud / Tagsの保存形式は維持。
- `lyrictube.library.v3` / Data Schema 4は変更なし。
- JavaScript構文、JSON/Static検証、NodeロジックテストをCIで確認する。
- 実YouTube / MP3 / MP4再生とモバイル実機操作はGitHub Pagesデプロイ後の実ブラウザ確認が必要。

## v0.11.0 ざっくり自動同期（2026-08-30）

### 変更した内容

- 同期エディタに基準点方式の自動補間を追加。
- `今の時間（基準点）` を2〜5か所程度だけ設定し、その間を自動で時間合わせできるようにした。
- 元LRCの時間差を利用する区間伸縮方式を採用し、単純な等間隔配置による不自然さを減らした。
- 元同期が無い区間のみ行数ベースの均等補間を使用。
- 基準点行を視覚的に表示し、基準点クリア機能を追加。
- 補間処理をPure utilityへ分離し自動テストを追加。

### 変更ファイル

- `app.js`
- `index.html`
- `styles.css`
- `sync-interpolation.js`
- `tests/sync-interpolation.test.js`
- `version.js`
- `data/defaults.json`
- `README.md`
- `docs/LYRICS.md`
- `docs/ARCHITECTURE.md`
- `docs/CHANGELOG.md`
- `.github/workflows/validate-js.yml`

### 崩していない仕様

- 既存の1行ずつの手動同期は維持。
- 動画専用LRCの保存形式は変更なし。
- `lyrictube.library.v3` / Data Schema 4を維持。
- YouTube / Local Mediaの両方から同じ同期エディタを利用する構成を維持。

### 確認

- JavaScript構文チェックを実施。
- JSON構文と静的参照検証を実施。
- 補間エンジンのNodeテストを実施。
- GitHub Pages上での実動画を使った操作感はデプロイ後に実ブラウザ確認が必要。

## v0.10.2 追加修正（2026-08-30）

- 元のWebPアイコンをGit履歴から復元。
- 意図せず追加されていた代替SVGアイコンの利用を停止。
- VReviewを再確認し、バージョン表示を `MY MUSIC · v0.10.2` 形式へ変更。
- 表示Versionと内部Buildの分離方針は維持。
- GitHub Pages上の実ブラウザ表示はデプロイ後にユーザー確認が必要。

# 作業報告書

## v0.10.1 / 2026-08-30

- `?` ヘルプボタン追加後にサイドバー下部が3列の旧CSSを継承し、`読み込み` が2段目へ落ちる問題を修正。
- 4項目を1行で収めるグリッドへ変更し、モバイル側の旧2列上書きも整合。
- GitHub Actionsの静的検証対象として通常どおり確認する。

## v0.10.0 / 2026-08-29 — 土台再構築

### 目的

機能追加を続けたことで重複実装・バージョン混在・同期処理の二重化・旧Local Audio依存・検証不足が増えていたため、既存機能を維持しながら土台を整理しました。

### 今回変更した内容

#### バージョン

- ユーザー向けSemVerを `v0.10.0` に更新
- Buildを `20260829-10` に更新
- `version.js` をバージョン情報の正本に変更
- 旧 `v35 / v36 / GH v31` を現行UIから撤去
- Browser titleを `LyricTube` に統一

#### Static / UI

- index.html内のBase64画像を撤去
- `assets/lyrictube-icon.svg` へ分離
- index.htmlを約41KBから約32KBへ縮小

#### データ

- `library-schema.js` を追加
- `data/library.schema.json` を追加
- Data Schema 4を導入
- 旧ライブラリを読み込み時に正規化
- `lyricsProvider / lyricsProviderId / tagIds / localmedia` 等をSchemaへ整理

#### Cloud同期

- Cloud書き込みを `cloud-sync.js` だけに一本化
- `site-shell.js` の旧全体保存Writerを無効化
- 差分同期QueueをlocalStorageへ保持
- 通信失敗時の再送を追加
- `visibilitychange / pagehide` でkeepalive保存を試す

#### ログイン

- 前回成功したアカウント名を端末に記憶
- 次回はパスワードだけでログインできるUIへ統合
- `login-memory.js` の後付けPatchを廃止
- パスワードは保存しない
- Supabase側で同一アカウントへの失敗回数制限を追加
  - 10分以内に5回失敗 → 15分ブロック
  - 成功時はカウンター削除
- テスト用アカウント名でRate limitが5回目に有効になることをDB側で確認

#### Local Media

- `local-audio.js / local-audio.css` runtimeを廃止
- MP3 / MP4 / WebMを `local-media.js` へ一本化
- 旧IndexedDB `lyrictube.localAudio.v1` から新DBへの移行処理を追加
- 別端末でファイルが無い場合の再登録導線を維持
- 設定画面へ端末ファイル使用量表示を追加
- timeupdateによるUI更新を約125ms間隔へ抑制

#### 歌詞

- LRCLIB + SyncLRC + lyrics.ovh構成を現行仕様として整理
- Provider metadataをData Schemaへ含めた

#### Core構造

- `window.LyricTubeCore` Facadeを追加
- 今後の追加モジュールがraw global stateへ直接依存しすぎない移行を開始

#### CI

GitHub Actionsを強化しました。

- 全JavaScriptへ `node --check`
- Python保守Toolの構文チェック
- 全JSON構文チェック
- index.htmlのローカルAsset参照チェック
- 廃止Local Audio runtimeの再混入検出
- Base64画像再埋め込み検出

v0.10.0 refactor後に、実際にMaintenance workflow内で全JS構文・Static validationが成功しています。

### 変更した主なファイル

- `index.html`
- `app.js`
- `site-shell.js`
- `profile-data.js`
- `cloud-sync.js`
- `local-media.js`
- `tags.js`
- `README.md`
- `作業報告書.md`
- `.github/workflows/validate-js.yml`
- `.github/workflows/apply-chatgpt-patch.yml`

### 追加した主なファイル

- `version.js`
- `library-schema.js`
- `auth-ui.css`
- `assets/lyrictube-icon.svg`
- `data/library.schema.json`
- `docs/ARCHITECTURE.md`
- `docs/CLOUD.md`
- `docs/DATA_SCHEMA.md`
- `docs/LYRICS.md`
- `docs/STORAGE.md`
- `docs/CHANGELOG.md`
- `docs/KNOWN_ISSUES.md`

### 削除したもの

- `local-audio.js`
- `local-audio.css`
- `login-memory.js`
- `version-meta.js`

### 確認済み

- v0.10.0 Maintenance workflow成功
- Refactor実行成功
- 全JS `node --check` 成功
- JSON構文検証成功
- Static asset参照検証成功
- Local Media refactor直後に見つかった末尾 `&&` の構文ミスを修正し、再検証成功
- Supabase login rate limitが5回失敗でblock状態になることをDB側で確認
- GitHub Pages build成功

### 確認できていないこと

生成環境からユーザーの実ブラウザを直接操作できないため、以下は未確認です。

- 実YouTube再生
- 実MP3再生
- 実MP4再生
- iPhone / Android実機
- LRCLIB / SyncLRC / lyrics.ovhを使った複数曲での実検索
- 長時間利用時のメモリ使用量

### 未完了

`app.js` はまだ約133KBあり、長期的な保守性は改善余地があります。

ただし一括分割すると再生・同期・ライブラリ互換を同時に壊すリスクが高いため、v0.10.0では重複ランタイム・同期・保存・認証・Version・検証基盤を先に修正しました。

次回以降は `docs/ARCHITECTURE.md` の順序で段階的に `app.js` を分割します。

旧v03〜v35の詳細履歴はGit履歴に残っています。現行仕様はREADMEとdocsを正とします。