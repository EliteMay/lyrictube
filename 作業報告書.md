# 作業報告書

## v14 起動修正 / 2026-08-18

### 対象
- Kimiで改善されたv14の機能・UI・データ仕様は変更しない。
- 開かない問題のため、起動処理だけ修正。

### 確認結果
- `server.mjs` 本体はローカルで正常起動。
- `/api/health` は `version: v14` / HTTP 200。
- `/` と `app.js` もHTTP 200で配信できることを確認。
- 元の `start.bat` はPowerShellランチャー経由だったため、起動経路を簡略化。

### 修正
- `start.bat` から `server.mjs` をNode.jsで直接起動。
- サーバーが4173〜4190から実際に確保したポートを自動でブラウザ表示。
- `cd /d "%~dp0"` とし、フォルダ名に空白があっても起動できるよう修正。
- `app.js` 内部の版表記だけv13→v14へ整合。機能ロジックは変更なし。
- `launcher.ps1` は手動起動用の予備として残し、エラー処理のみ安定化。

### 完了条件
- `start.bat` からNode.jsサーバーが起動する。
- health/index/app.jsが取得できる。
- Kimi版の既存UI・機能を変更しない。

## v14 / 2026-08-18 欠落関数の補完・完成版

### 不具合
v13のapp.jsは、イベント登録で参照している以下の関数が未定義のままで、該当ボタンを押すとReferenceErrorで動作しない状態でした。

- お気に入り切替: `toggleFavorite`
- 動画バージョン追加/編集/削除: `openVersionDialog` `saveVersionForm` `deleteVersion`
- 曲開始/終了・範囲リセット: `setRange` `resetRange`
- スキップ区間の記録: `markSkipStart` `markSkipEnd`
- 歌詞オフセット: `updateOffset`
- 同期エディタ一式: `openSyncEditor` `renderSyncEditor` `resetSyncDraft` `saveSyncDraft` `useSharedSync` `undoSyncChange` `pushSyncUndo` `updateSyncUndoButton`
- 同期エディタの再生操作: `seekSyncPlayer` `nudgeSyncPlayer` `toggleSyncPlayback` `updateSyncTransport` `syncRelativeSeconds` `getPlayerDuration`

### 修正
- 上記すべてを実装。v13までのREADME記載の仕様通りに動作します。
- 同期エディタの「時間を0にする」は全行まとめてUndo可能。
- 動画を別URLへ差し替えた場合、旧動画の開始/終了/スキップ/動画専用同期を自動リセット。
- 最後の1本の動画を削除する場合は、確認のうえ曲ごと削除。
- 同じ動画IDのバージョン重複追加時に確認を表示。
- 版表示をv14へ統一（タイトル/バッジ/クエリ/health API）。

### 同梱の補完
- アップロード一式に含まれていなかった `server.mjs` `launcher.ps1` `start.bat` `data/defaults.json` を、README記載の仕様（ポート4173〜4190、LRCLIB/oEmbed中継、no-store配信、healthのversion確認）に沿って再構築しました。
- 既にこれらのファイルをお持ちの場合は、既存のものを優先しても動作します（API仕様はv09以降互換）。

### 動作確認
- `node --check` でapp.js / server.mjsの構文検査を通過。
- ローカル起動で `/api/health`（version: v14）、静的配信、LRCLIB検索中継（KING/Kanariaで20件）を確認。
- ヘッドレスブラウザで初期描画・初期化処理がエラーなく完了することを確認。
- YouTube oEmbed通信は検証環境のネットワーク制約で未確認。失敗時はブラウザ内Playerフォールバックが動きます。

### 互換性
- ライブラリ保存形式（lyrictube.library.v3）は変更なし。v03〜v13のデータをそのまま利用可能。

## v03 / 2026-08-17

### 仕様変更
v02までの「1曲 = 1YouTube動画」から、v03は「1曲 = 原曲情報 + 複数動画バージョン」へデータ構造を変更しました。

変更理由:
- 歌ってみた、Live、FIRST TAKE、別MVを同じ曲として扱うため。
- 歌詞は原曲で共有し、動画固有の開始位置や同期だけ分離するため。

### 実装
- 複数動画バージョン管理。
- 動画タイプ・表示名・歌唱者保存。
- Coverでも原曲名/原曲アーティストでLRCLIB検索。
- 曲開始位置。
- 曲終了位置。
- 複数自動スキップ区間。
- 動画ごとの歌詞オフセット。
- 動画ごとの専用LRC同期。
- 原曲LRCへ戻す機能。
- プレイリスト作成/削除/曲追加。
- 表示中プレイリストを基準に次曲/前曲。
- シャッフル。
- 全曲リピート / 1曲リピート。
- お気に入り。
- 最近聴いた曲。
- 再生回数/最終再生日時保存。
- 歌詞の大表示。
- キーボードショートカット。
- v01/v02 localStorageからv03形式への自動移行。
- v03 JSON書き出し/読み込み。

### 外部仕様確認
- LRCLIB公式ドキュメントで `/api/search` の検索仕様を確認。
- YouTube公式IFrame Player APIでJavaScriptから再生制御・現在時刻取得・シーク・状態イベントが利用できることを確認。

### 互換性
- 旧キー `lyrictube.songs.v1` を検出した場合、各旧曲を「原曲/MV」1バージョンとして移行。
- v02のlyricsOffsetは意味方向が逆だったため符号を反転して移行。

### 起動
- v02で修正した start.bat + launcher.ps1 + health check方式を維持。
- Node.js v18以上。
- ポート4173が使用中なら4174〜4190を自動選択。

### 未確認 / 制限
- 検証環境ではユーザーのWindowsブラウザ上での実クリック操作までは実施できない。
- YouTube埋め込み可否は動画ごとの投稿者設定に依存。
- LRCLIB検索結果の有無・同期品質は曲ごとの登録状況に依存。
- Coverでテンポそのものが変わる場合、自動オフセットだけでは合わないため動画専用同期が必要。

### 崩してはいけない仕様
- 曲と動画バージョンの分離。
- 動画ごとの startTime/endTime/skipSegments/lyricsOffset/customSyncedLyrics。
- YouTube動画を直接保存しない。
- APIキー不要・npm依存なし。
- READMEと作業報告書を変更時に更新。


## v04 / 2026-08-17 動画情報取得修正

### 不具合
- v03で「動画情報」を押しても、v02のように曲名などが分かりやすく自動入力されない状態を確認。

### 修正
- ボタンを押した場合はYouTube情報を明示的に入力欄へ反映する仕様へ変更。
- YouTube URLを通常のwatch URLへ正規化してからoEmbedへ問い合わせるよう変更。
- youtu.be / Shorts / music.youtube.com系でも動画IDを基準に取得。
- `Artist - Song` と `Song / Artist` の簡易解析を追加。
- `歌ってみた / Cover / THE FIRST TAKE / Live / Acoustic` の動画タイプ自動判定を追加。
- 初回動画では投稿者を「歌唱者 / 投稿者」に自動入力。
- 別バージョン追加時も表示名・投稿者・タイプを自動入力。
- 取得失敗時のサーバーログを強化。

### 維持
- データ形式はv03と同じため、v03で登録したライブラリはそのまま利用可能。


## v05 / 2026-08-17 動画情報取得・キャッシュ修正

### 判明した原因
- v04のHTMLタイトルが `LyricTube v03` のまま残っていた。
- README先頭もv03表記のままだった。
- server.mjsがJS/CSSへ `Cache-Control: public, max-age=300` を返していた。
- このためv03/v04を同じローカルURLで短時間に開くと、古いapp.jsが最大5分再利用される可能性があった。

### 修正
- UIにv05バッジ追加。
- タイトルをLyricTube v05へ修正。
- 全ローカル静的ファイルをno-store化。
- JS/CSSにv05クエリを付与。
- health checkにアプリバージョンを追加。
- launcher.ps1はv05サーバーであることまで確認してからブラウザを開く。
- YouTube情報取得を `/api/youtube-info` へ刷新。
- サーバー取得を oEmbed → YouTube動画ページ の2段階化。
- サーバー取得失敗時はブラウザ内YouTube Playerによるタイトル/投稿者取得をフォールバックとして追加。
- 原曲動画でタイトルからアーティストを分離できない場合は、投稿チャンネル名を原曲アーティスト候補として入力。
- サーバーとランチャーの二重ブラウザ起動を解消。

### 互換性
- 曲ライブラリのデータ形式自体はv03形式を維持。
- v03/v04で保存したデータは同じlocalStorageキーを利用するため引き続き利用可能。


## v06 / 2026-08-17 歌詞手動取得導線

### 追加
- `Googleで歌詞検索` ボタンを追加。
- 入力中の曲名・原曲アーティストを使い `曲名 アーティスト 歌詞` でGoogle検索。
- Googleタブから戻った際、歌詞欄へ自動フォーカス。
- `クリップボードから貼り付け` ボタンを追加。
- Clipboard API利用不可/拒否時はCtrl+Vを案内。
- LRCLIB検索結果0件時にGoogle検索利用を案内。
- UI/サーバー/ランチャーの版表示をv06へ統一。

### 方針
- 外部歌詞サイトの自動スクレイピングは行わない。
- 自動取得できない曲は、検索→ユーザーコピー→貼り付けを最短導線にする。


## v07 / 2026-08-17 歌詞検索0件対策

### 原因
- v06は原曲アーティスト欄に値があると、LRCLIBへ常に `track_name + artist_name` を送信していた。
- YouTube動画情報から自動入力した投稿者名やCover歌唱者が原曲アーティストとして不正確な場合、検索条件が狭くなり0件になりやすかった。

### 修正
- `曲名+アーティスト -> 曲名だけ -> q複合 -> q曲名だけ` の自動フォールバックを実装。
- サーバーから検索モードを返し、条件をゆるめて見つかった場合はUIで通知。
- LRCLIB通信ログに検索方式と件数を記録。
- v06のGoogle検索/クリップボード貼り付け機能を維持。

### 未確認
- 実際のLRCLIB外部通信は実行環境のネットワーク制約によりローカル自動テスト不可。API仕様は公式ドキュメントで確認。


## v08 / 2026-08-17 歌詞検索v1互換化

### 判断
- v1では同じ「嵐 / Monster」で歌詞候補が表示できていたとの確認あり。
- そのためLRCLIB自体や曲名ではなく、後続版で変更した検索ロジックによる回帰不具合の可能性を優先。

### 修正
- v07の複数検索・レスポンスラップ処理を撤去。
- app.jsの歌詞検索をv1相当へ戻した。
- server.mjsのLRCLIBプロキシをv1相当へ戻した。
- LRCLIBから返る配列を加工せずブラウザへ返す。
- Google歌詞検索などv06/v07の補助機能は維持。


## v09 / 2026-08-17

### 修正1: 歌ってみたで歌詞が出ない
- MVのv1互換検索は維持。
- Cover系で通常検索0件時のみ追加検索する方式へ変更。
- LRCLIBプロキシへ `q` 自由検索対応を追加。
- Cover投稿者を原曲アーティストへ誤入力しないよう変更。
- YouTube元タイトルを一時保持し、Cover装飾を除いた検索候補にも利用。

### 修正2: 歌詞検索結果をコピーできない
- 歌詞候補カード全体のbutton化を廃止。
- `この歌詞を使う` と `歌詞をコピー` を独立。
- 全歌詞を展開して手動選択できるプレビューを追加。
- Clipboard API失敗時のfallbackコピーを追加。

### 修正3: UIが背面に表示される
- 歌詞検索時の多重modalを廃止。
- 親の曲編集ダイアログを閉じてから検索結果をshowModalし、終了後に親へ戻す。
- modal表示中のtoastをtop-layer内へ表示。
- 全画面歌詞と編集modalの競合を避けるため、modalを開く際は全画面歌詞を解除。

### 維持
- v08までの保存データ形式を維持。
- MVの歌詞検索方法を壊さない。
- Google歌詞検索、クリップボード貼り付け、複数動画、開始/終了、スキップ、プレイリストを維持。


## v10 / 2026-08-17 Monster回帰修正

### 原因比較
- v1の動画情報取得はYouTubeタイトルをそのまま曲名へ入力していた。
- 後続版はYouTubeタイトルを解析して曲名/アーティストを分離していた。
- 歌詞検索APIが同じでも、送信文字列がv1と後続版で一致していなかった。

### 修正
- 動画ごとに `rawYoutubeTitle` / `rawYoutubeAuthor` を保存。
- 通常検索0件時に、v1と同じ生タイトル+投稿者検索を必ず実行。
- さらに0件なら曲名のみ検索。
- Cover系追加検索はその後に限定。
- MV検索とCover検索のルートを明示的に分離。
- v09の歌詞コピー/前面ダイアログ修正は維持。

### 既存データ
- 既存ライブラリ形式と互換。
- v09以前の既存動画はraw情報未保存の場合があるため、
  必要なら動画編集で「動画情報を取得」→保存を1回行う。


## v11 / 2026-08-17 歌詞同期エディタ改善

### 要望
- 歌詞時間合わせ画面に動画シークバーが欲しい。
- 1回の押し間違いで修正しづらい問題を解消する。

### 実装
- 同期ダイアログ上部に動画シークバーを追加。
- 動画現在時刻 / 動画総時間 / 曲開始からの相対時刻を表示。
- 再生/一時停止、±1秒、±5秒、曲開始へ移動を追加。
- 直前の行時間変更をUndo可能にした。
- 「時間を0にする」もUndo可能。
- 歌詞行ごとに「この時間へ」を追加。
- 「今の時間」は何度でも同じ行へ上書き可能。
- 直接時間入力でもUndo履歴を作成。
- 同期エディタ表示中は終了位置処理・自動スキップを停止。

### 互換性
- ライブラリ保存形式は変更していない。
- v10以前の曲・歌詞同期データをそのまま利用可能。


## v12 / 2026-08-17 歌詞編集と同期時間の連動修正

### 不具合
- 時間合わせ後に歌詞本文を編集しても、`customSyncedLyrics` に旧本文が残り、表示へ反映されなかった。
- 通常歌詞で保存し直すと共有 `syncedLyrics` の時間情報も失われる可能性があった。

### 修正
- `rebaseLrcTextKeepingTimes` を追加。
- 同じ行数の修正はタイムスタンプを完全維持して本文のみ交換。
- 行数変更時はLCSで旧行と新行を照合。
- 既存行の時刻を維持し、追加行は前後の時刻から補間。
- 全動画バージョンの `customSyncedLyrics` へ歌詞修正を反映。
- 共有 `syncedLyrics` も平文編集時は時間を維持。
- 新しいLRCを貼った場合は新しい共有時刻を優先。
- 歌詞を空にした場合のみ同期データも明示的に削除。

### 互換性
- 保存データのキー構造は変更していない。
- v11以前の曲、動画専用同期、開始/終了、スキップ、プレイリストをそのまま利用可能。


## v13 / 2026-08-17 旧JSONインポート修正

### 不具合
- v01/v02のexport JSONはオブジェクト形式 `{version, exportedAt, songs}`。
- v12のimportは「JSON自体が配列」の場合しか旧形式へ変換していなかった。
- 旧曲をnormalizeLibraryへ渡すとversionsが空配列となり、動画を正常復元できなかった。

### 修正
- `detectImportFormat` を追加。
- v01/v02配列、v01/v02export object、v03+形式を自動判定。
- `migrateLegacySongs` で旧1曲=1動画形式をv13のversionsへ変換。
- BOM付きJSON対応。
- `library` ラップ形式対応。
- 読み込み前に形式/曲数/動画数を表示。
- エラー時に具体的な失敗理由を表示。
- 壊れた空動画を除外しつつ、歌詞のみの曲データは保持。

### 互換性
- v03〜v12の現行ライブラリ形式はnormalizeLibraryで従来通り読み込み。
- v12までの本体機能・保存形式は維持。


## v15 / 2026-08-18

### デザイン
- Kimi v14 fixをベースに見た目を刷新。
- Now Playingのサムネイル・動画タイプ・再生回数を追加。
- サムネイルを低濃度の背景演出へ利用。
- 動画IDから軽量にアクセント色を決定。
- ライブラリ選択状態、プレイヤー、歌詞表示の視認性を改善。
- 固定下部プレイヤーを追加。

### 機能
- スマート一覧「歌詞未登録 / 同期未設定 / Coverあり」を追加。
- 再生キュー表示を追加。
- 音量保存を追加。

### 自動スクロール不具合・操作性
- 歌詞ヘッダーをsticky化し、ON/OFFボタンを常時操作可能にした。
- 手動スクロールを検知した場合、8秒間自動追従を停止。
- 一時停止状態をボタン表示へ反映。
- 一時停止中にOFFボタンを押せるため、自動スクロールに操作を邪魔されない。

### 互換性
- localStorageキーと曲データ形式は変更なし。
- v14以前の保存データをそのまま使用可能。


## v16 / 2026-08-18 プレイリスト整理中の再生維持

### 要望
- 曲を聴きながら別の曲をプレイリストへ追加したい。
- プレイリスト追加対象を選ぶために曲をクリックすると再生曲まで変わる問題を解消する。

### 実装
- ライブラリの各曲行にプレイリストQuick Add `＋` を追加。
- 通常の曲選択ボタンとQuick AddボタンをDOM上も分離。
- `playlistTargetSongId` を追加。
- プレイリストダイアログは対象曲を `getSong()` ではなく独立targetから取得。
- チェック変更時に再生中の曲・動画バージョン・YouTube Playerを変更しない。
- 新規プレイリスト作成時もQuick Add対象曲を自動追加可能。
- プレイリストダイアログ内に「現在再生中の曲は切り替わらない」旨を表示。

### UI
- Quick AddはPCでは通常時薄く、曲行へマウスを合わせると表示。
- モバイル/狭幅では常時見つけやすい濃さで表示。
- 既存の曲行クリック領域は従来どおり再生選択として維持。

### 互換性
- データ形式変更なし。
- v15以前の保存データをそのまま利用可能。


## v17 / 2026-08-18 大型曲選択ページ + 歌詞先頭修正

### 曲選択UI
- `再生画面 / 曲を探す` 切替を追加。
- 大型カードグリッドを追加。
- 大きいYouTubeサムネイルから再生可能。
- 再生しても大型ブラウズ画面に残る。
- 下固定プレイヤーとの併用を前提にした。
- 大型画面にもプレイリストQuick Addを追加。
- サイドバー検索と大型検索を同期。
- お気に入り / 最近 / Cover / 歌詞未登録 / 同期未設定を大型画面から切替可能。

### 歌詞不具合
- `renderLyrics()` 時に `lyricsView.scrollTop = 0` を追加。
- 歌詞上端maskを透明から不透明へ変更。
- 先頭歌詞へ追加余白。
- `scrollIntoView()` を廃止。
- 自動追従を `lyricsView.scrollTo()` に変更し、外側ページのスクロールを発生させないようにした。

### 互換性
- Song / Version / Playlist構造変更なし。
- localStorageキー変更なし。
- settingsへ `mainPage` を追加するだけなので旧データもnormalizeLibraryで利用可能。


## v18 / 2026-08-18 プレイリスト追加ダイアログ修正

### 不具合
- v16/v17でQuick Addの `＋` を押してもプレイリスト追加画面が開かない。
- 原因は `openPlaylistDialog()` 内の未定義 `escapeHtml()` 呼び出し。
- 実行時に `ReferenceError` となり `showModal()` まで到達していなかった。

### 修正
- `escapeHtml()` を使用しないDOM生成へ変更。
- 曲名は `textContent` / `createTextNode` で表示。
- 再生維持ロジック `playlistTargetSongId` は変更なし。
- v17の大型曲選択ページ、歌詞先頭修正などは変更なし。

### 確認
- `escapeHtml` 参照がプロジェクト内に0件であることを確認。
- app.js / server.mjs の構文確認。
- v18 health check。
- Quick Addが `openPlaylistDialog(song.id)` を呼び、再生変更処理を含まないことを静的確認。


## v19 / 2026-08-18 YouTube Data API検索

### 要件定義
- URLコピーを減らしLyricTube内でYouTube動画を探す。
- APIキーはユーザーPCだけに保存。
- 既存データ形式と手動URL登録を維持。
- npm依存は追加しない。

### 実装
サーバー:
- GET `/api/youtube/settings`
- POST `/api/youtube/settings`
- DELETE `/api/youtube/settings`
- POST `/api/youtube/settings/test`
- GET `/api/youtube/search?q=...`

Windows APIキー保存先:
`%APPDATA%\LyricTube\settings.json`

検索:
- 最大12件
- type=video
- 埋め込み可能条件
- 詳細APIからduration / status / statistics
- embeddable=falseを除外

UI:
- 左下 `設定`
- APIキー保存 / 削除 / 接続確認 / マスク表示
- 大型曲選択ページからYouTube検索
- 新規曲フォームからYouTube検索
- 別バージョンフォームからYouTube検索
- サムネ付き検索候補

### セキュリティ・データ
- APIキーをHTML/JS/defaults/曲JSONへ直書きしない。
- ブラウザへ保存キー本体を返さない。
- 設定変更APIは127.0.0.1/localhost由来のみ許可。
- サーバーは従来通り127.0.0.1だけで待受。

### 互換性
- localStorageキー変更なし。
- Song / Version / Playlist構造変更なし。
- APIキーがなくても従来機能は動く。


## v20 / 2026-08-18 歌詞先頭表示の競合修正

### 再現原因
曲切替の処理順:
1. 新しい歌詞を描画してscrollTop=0。
2. YouTube Playerはまだ一瞬前の動画を保持。
3. 180ms周期のplaybackTickが前動画のcurrentTimeを取得。
4. その時間を新歌詞に適用し、自動スクロール。
5. 新動画が開始位置へ切替。
6. 1行目開始前ならindex=-1。
7. 旧コードはindex=-1で先頭位置を回復しないケースがあった。

### 修正
- `lyricVideoSwitchPending` を追加。
- `playerVideoIdSafe` でPlayer動画IDを確認。
- `lyricPlayerReadyForSelectedVideo` で新動画への切替完了を判定。
- `resetLyricsViewport` を追加し、描画後2フレームまで先頭位置を保証。
- `idx < 0` の場合に自動スクロールONならscrollTop=0へ復旧。
- `idx === 0` の場合も不正なscrollTopを検出して復旧。
- renderLyrics、selectSong、selectVersion、loadSelectedVideoから先頭位置を保護。
- CSS上端maskを完全に撤去。

### 確認
- v20 app.js / server.mjs構文確認。
- v20 health check。
- 旧 `scrollIntoView` を歌詞追従処理で使用していないことを確認。
- v19のYouTube検索 / API設定 / プレイリスト / 大型曲選択画面を維持。


## v21 / 2026-08-18 歌詞先頭のレイアウト修正

### 症状
- 通常表示で先頭歌詞が表示されない。
- 上限までスクロールしていて、それ以上上に動かせない。
- 大きく表示では先頭歌詞が表示される。

### 原因
v15で追加した
`.lyrics-head { position:sticky; top:95px; }`
が歌詞本文へ重なっていた。

fullscreenではtop:0だったため表示できていた。

### 修正
- stickyを削除。
- top:95pxを削除。
- headerを通常Flexフローへ戻した。
- lyricsViewへmin-height:0 / overflow-y:autoを明示。
- 先頭余白を整理。
- JS側の先頭位置保証は補助として維持・追加。

### 既存機能
v20までのYouTube検索、API設定、プレイリスト、大型曲選択、歌詞同期、データ形式は変更なし。


## v22 / 2026-08-18 間奏マーカー

### 要望
Google等から取得した歌詞には間奏の音符行がないため、
同期時間調整画面から手動で追加したい。

### 実装
- 同期トランスポートへ `♪ 間奏を追加`。
- 現在動画時刻を曲基準時刻へ変換して `♪` を追加。
- 設定済み歌詞時間を使い、Google歌詞の未設定0秒行を考慮して挿入位置を推定。
- 間奏行を専用色で表示。
- 間奏行へ `今の時間` / `この時間へ` / `♪を削除`。
- Undoへinsert/remove対応を追加。
- 0.25秒以内の重複 `♪` は誤操作として追加を抑止。

### 歌詞編集との互換性
- `rebaseLrcTextKeepingTimes` で `♪ / ♫ / ♬ / ♩` の同期マーカーを通常歌詞行から分離。
- 歌詞本文修正後に元のマーカー時刻で再結合。
- 動画専用間奏を保持したまま誤字修正や歌詞修正が可能。

### 既存機能
v21までの歌詞先頭修正、YouTube検索、API設定、プレイリスト、大型曲選択、同期機能、データ形式を維持。


## v26 / 2026-08-24 歌詞検索回帰・Coverアーティスト修正

### 原因
- LRCLIB中継がChrome User-Agentを送信。
- 12秒timeoutが現在のLRCLIB応答には短い。
- 1回のHTTP失敗でfallback全体が中断。
- 429 Retry-After未対応。
- Coverでも `Artist - Song` 左側を原曲アーティストへ反映していた。

### 修正
- LyricTube識別ヘッダー。
- 30秒timeout。
- 一時障害1回retry。
- 429停止/待ち時間表示。
- 350ms throttle。
- 10分cache。
- 保存済みlrclibId直接取得。
- 多段検索診断表示。
- Cover歌唱者と原曲アーティスト分離。

### 確認
- app.js / server.mjs構文。
- mock LRCLIBで保存済みID取得。
- mock LRCLIBで一時500後retry。
- LRCLIB識別ヘッダー。
- Cover解析4パターン。
- local server health。
- APIキー非混入。

### 未確認
生成環境は外部LRCLIBへDNS接続できないため、本番LRCLIB実検索は未確認。


## v27 / 2026-08-24 再生・ダイアログ回帰修正

### ユーザー報告
- 曲が再生されなくなった。
- ×ボタンが反応しないことが多い。

### 原因特定
v26の範囲置換によりGoogle歌詞検索補助関数と
YouTube Playerメタデータfallback関数が消えていた。

イベント登録中:
`els.googleLyricsBtn.addEventListener("click", openGoogleLyricsSearch)`

で未定義参照となり、以降の処理が停止。

### 影響
未実行になっていたもの:
- 歌詞検索ダイアログ閉じる
- 動画編集ダイアログ閉じる
- 同期ダイアログ閉じる
- プレイリスト閉じる
- キュー閉じる
- ショートカット閉じる
- 下部プレイヤー操作
- 最終bootstrap
- YouTube IFrame API読込

### 修正
- 消えた関数をすべて復元。
- 主要×ボタンのcapture fallback。
- core bootstrapをイベント登録より前へ移動。
- 元Kimi版とv27の関数一覧を比較し、元版に存在した関数が欠落していないことを検査。
- 直接event handlerとして使われる関数が定義済みか静的検査。

### 確認
- app.js/server.mjs構文。
- v27 local server health。
- 元版関数集合との差分。
- 必須DOM ID。
- APIキー非混入。


## GitHub v28 / 2026-08-27 静的GitHub Pages化

### 今回変更した内容
- ローカルNodeサーバー構成からGitHub Pages静的構成へ変更。
- LRCLIB検索をserver中継からブラウザ直接fetchへ変更。
- LRCLIBは逐次検索、350ms待機、30秒timeout、一時障害1回retry、10分メモリcache。
- 保存済みlrclibIdの直接取得を維持。
- YouTube動画情報取得はIFrame Player APIのみ。
- テーマ/歌詞サイズ/表示設定は維持。

### 削除
- server.mjs
- start.bat
- launcher.ps1
- YouTube Data APIキー設定UI
- YouTube Data APIサイト内動画検索UI
- ローカル `/api/*` 依存

### 維持
YouTube再生、曲/動画バージョン、Cover/Live、プレイリスト、お気に入り、
LRCLIB歌詞検索、Google歌詞検索、LRC同期、間奏♪、各行時間微調整、
JSONバックアップ、localStorage互換。

### データ移行
GitHub Pagesと旧ローカル版はoriginが違うため、旧v27からJSON書き出し→GitHub版読み込みが必要。

### 確認
- app.js構文
- HTML ID重複
- 相対 `/api/` 呼び出し除去
- server/start/launcher削除
- GitHub Pages相対パス
- `.nojekyll` / `.gitignore`

### 未確認
生成環境から本番LRCLIBへの実通信は未確認。


## GitHub v29 / 2026-08-27 ブランド・設定・ヘルプ強化

### 今回変更した内容
- 生成済みLyricTubeロゴ/アイコンをサイトへ組み込み。
- favicon / Apple touch iconを設定。
- 曲を探す画面へブランドロゴを追加。
- UIを少し固め、余白・境界線・カードの陰影・情報階層を整理。
- 設定へ起動画面、コンパクト表示、サムネイル表示、ガラス質感、動き抑制、ヘルプ導線を追加。
- 起動画面設定を現在表示中のページとは別に保存するよう修正。
- ？ヘルプダイアログを追加。再生、歌詞、同期、設定の説明を表示。
- PowerToys Text Extractorの `Win + Shift + T` → 範囲選択 → `Ctrl + V` の歌詞OCR手順を案内。
- 同期エディタで行を選択し、`Shift + T` でその行へ現在時間を打刻。
- ヘルプ表示OFF、サムネイルOFFなどの設定をlocalStorageへ保持。

### 崩していない仕様
- `lyrictube.library.v3` のデータ形式。
- YouTube IFrame再生。
- LRCLIB直接検索。
- プレイリスト/お気に入り/最近聴いた曲。
- 間奏♪、同期Undo、歌詞先頭修正。

### 確認予定/未確認
- GitHub Pages実URLでの最終表示。
- 実YouTube動画再生。
- 実LRCLIB検索。
